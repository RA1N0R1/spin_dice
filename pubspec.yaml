name: spin_dice
description: "Игра в кубик с клиент-серверной архитектурой"
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5  # Для HTTP-запросов к серверу
  provider: ^6.1.1  # Для управления состоянием (опционально)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0